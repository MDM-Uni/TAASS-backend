apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: frontend-apigateway-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/frontend-apigateway-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/frontend-apigateway-net: "true"
